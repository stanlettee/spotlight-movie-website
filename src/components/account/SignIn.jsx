import styles from "./styles/SignIn.module.css"

export const SignIn = () => {
    return <section className={styles.section}>
        <div className="container">
            <h2 className={styles.heading}>Log into your account</h2>
        </div>
    </section>
}