import styles from "./styles/SignUp.module.css"

export const SignUp = () => {
    return <section className={styles.section}>
        <div className="container">
            <h2 className={styles.heading}>Authorise your account</h2>
        </div>
    </section>
}